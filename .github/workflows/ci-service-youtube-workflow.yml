name: ci-service-youtube-workflow

on:
  push:
    branches: [ "main" ]
    paths:
      - services/codecrew-service-youtube/**
      - build.gradle.kts
      - settings.gradle.kts
      - build-logic/**
      - gradle/**
      - module/**
      - ci-service-youtube-workflow.yml

permissions:
  contents: read
  packages: write

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout source
        uses: actions/checkout@v4

      - name: Set up JDK 25
        uses: actions/setup-java@v4
        with:
          java-version: "25"
          distribution: "temurin"
          cache: "gradle"

      - name: Build with Gradle
        run: |
          chmod +x ./gradlew
          ./gradlew :services:codecrew-service-youtube:bootJar -x test

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build & Push Image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: services/codecrew-service-youtube/Dockerfile
          push: true
          tags: |
            ghcr.io/${{ github.repository_owner }}/youtube-service:latest
            ghcr.io/${{ github.repository_owner }}/youtube-service:${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      # ===============================
      # GitOps 연동 단계
      # ===============================

      - name: Checkout GitOps repo
        uses: actions/checkout@v4
        with:
          repository: donghun0508/codecrew-gitops
          path: gitops
          token: ${{ secrets.GITOPS_TOKEN }}

      - name: Update image tag in GitOps repo
        run: |
          sed -i \
            "s/^  tag: .*/  tag: ${{ github.sha }}/" \
            gitops/services/youtube-service/prod.yaml

      - name: Commit and Push GitOps repo
        run: |
          cd gitops
          git config user.name "github-actions"
          git config user.email "github-actions@github.com"
          git add services/youtube-service/prod.yaml
          git commit -m "deploy(youtube-service): ${{ github.sha }}"
          git push

